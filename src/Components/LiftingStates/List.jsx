

const List = ({ list, check, query }) => {
    return (
        <>
            {list.map((item) => (

                <div>
                    <h2>{item.tite}</h2>
                    {}
                </div>

            ))}
        </>

    )
}

export default List;